1. There are many differences between var , let and const .
A Nice table explanation is given . 
var => var is a scoped function  and the value can be re-daclare and  the value can be changed. 

let => Let is a block scoped and can be not re declear and the value can be changed .


const =>const is a block scoped and can be re declear and can be the value changed .

2. Spread operator ... in js is a way to expand or spread elements of an array , object .

const arr1 = [1,2,3,4];
const arr2 = [...arr1, 4, 5];
console.log(arr2); // [1,2,3,4,4]



3. map() => In js map() is an array method that lets you transform each element of an array and cread a new array with the transformed values . 

filter() => In JS filter() is an array method that creats a new array containing only the elements that pass a certain test .

forEach() => In js foreach() is an array method that runs  a funciton on each element of an array . 



4. arrow function => In js an arrow funciton is a shorter sysntext for writing function . 

 Arrow function syntext: 
const square = x => x*x;


5. Strings writen with backtics ``  
Enbed variables or expressions ${}.
let name = "Susmi";
let text = `hello ${name};
console.log(text);// Susmi



